1. lồng thẻ i vào a để icon có thể đứng sát với chữ:
sai: <li><i class="ti-check"></i><a class="dropdown-item" href="#">Completed</a></li>
đúng: <li><a class="dropdown-item" href="#"><i class="ti-check"></i>Completed</a></li>
2. document.getElementsByClass() phải có số đánh thứ tự
sai: let notify = document.getElementsByClassName("notify");
đúng: let notify = document.getElementsByClassName("notify")[0];
3. Tạo biến cục bộ thay vì tạo biến bên trong hàm, do biến cục bộ chỉ khai báo 1 lần,điều này
khiến cho thay vì tạo task mới thì nó sẽ chỉ ghi đè nội dung của task cũ do nó tham chiếu 
đến cùng 1 biến
giải pháp: khai báo bên trong hàm, mỗi lần thực hiện hàm sẽ luôn tạo task mới
4. nên nhớ thêm event.preventDefault vào fuction khi được kích hoạt bằng button, nguyên nhân là mỗi khi nhấp vào button sẽ mặc định reload page lại
(tính chất mặc định của button).
5. nên để time là 1000 (1000 = 1s) trong các built-in có thời gian như set-interval, set-timeout
6. khi append child thì nên sử dụng class cha trực tiếp để ghi đè.
vd: cloneNode.querySelector(".task-container").appendChild(cancelStatus);
7. Trong trường hợp thiết kế website có tính năng hiện mục này và tắt hết mục kia thì nên display: none hết các mục để tạo homepage hoặc
chọn ra 1 mục để làm homepage.
8. Luôn để ý xem đâu là mục user nhấp vào, vd:
    <li class="nav-item">
       <a class="nav-link section" data-target="task-section" id="task" aria-current="page" href="#"><i class="ti-clipboard me-1"></i>Tasks</a>
    </li>
    đây mới là mục user nhấp vào.

            <div id="task-section" class="item-list">
                <ul id="task-list">
                    
                </ul>
            </div>
    còn đây là mục hiển thị

9. method .closet sẽ di chuyển một node kèm các phần tử bên trong nó, vì vậy khi không cần clone thì có thể
sử dụng .closet để di chuyển các node sang các vị trí khác nhau trong DOM